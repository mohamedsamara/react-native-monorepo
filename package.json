{
  "private": true,
  "name": "project",
  "version": "1.0.0",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "clean": "rm -rf packages/*/node_modules && rm -rf packages/*/yarn.lock && rm -rf ./node_modules && rm -rf yarn.lock",
    "reset": "yarn clean && yarn bootstrap"
  },
  "author": "Mohamed Samara (https://github.com/mohamedsamara)",
  "license": "MIT",
  "dependencies": {
    "metro-config": "^0.59.0"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}
